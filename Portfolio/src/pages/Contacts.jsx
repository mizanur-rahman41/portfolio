import React from "react";

const Contacts = () => (
  <section className="page contacts">
    <h2>Contacts</h2>
    {/* Contact info, social links as per design */}
  </section>
);

export default Contacts;
